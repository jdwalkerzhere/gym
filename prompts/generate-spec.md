# Spec-Supplied

Create a substantial executable environment at the allocated directory. Supply complete requirements and relevant datasets, fixtures, mock systems, incumbent/baseline behavior, evaluation machinery, and learner workspace. Require design, implementation/evaluation, tradeoffs, and recommendation. Make selected capabilities relevant through the problem rather than naming the intended choice; non-use may be correct.
